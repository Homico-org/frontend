// Twitter card image — re-uses the open-graph generator. Twitter accepts the
// same 1200×630 layout for the summary_large_image card.
export { default, alt, size, contentType, runtime } from "./opengraph-image";
